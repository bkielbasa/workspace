Workspace has been deployed.

HTTP API / CardDAV / CalDAV:
  https://{{ .Values.ingress.host }}/

Mail ports (via LoadBalancer {{ .Values.mailService.type }}):
  SMTP:  {{ .Values.ingress.host }}:{{ .Values.mailService.ports.smtp.port }}
  SMTPS: {{ .Values.ingress.host }}:{{ .Values.mailService.ports.smtps.port }}
  IMAP:  {{ .Values.ingress.host }}:{{ .Values.mailService.ports.imap.port }}
  IMAPS: {{ .Values.ingress.host }}:{{ .Values.mailService.ports.imaps.port }}

Autoconfiguration for mail clients:
  https://{{ .Values.ingress.host }}/.well-known/autoconfig/mail/config-v1.1.xml
